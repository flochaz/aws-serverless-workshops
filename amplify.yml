version: 0.1
frontend:
  phases:
    build:
      commands:
        - echo "Do nothing"
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: WebApplication/1_StaticWebHosting/website/
    files:
      - '**/*'

